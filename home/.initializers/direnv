# Initialize direnv depending on which shell we are in

# Disable messages
export DIRENV_LOG_FORMAT=

# If running zsh
if [ -n "$ZSH_VERSION" ]; then
	eval "$(direnv hook zsh)"

# If running bash
elif [ -n "$BASH_VERSION" ]; then
	eval "$(direnv hook bash)"
fi
